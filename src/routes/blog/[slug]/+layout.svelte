
<script>
	import { page } from '$app/stores';
  	import { MetaTags } from 'svelte-meta-tags';
	// A common pattern is to return SEO-related data from page load functions, 
	// then use it (as $page.data) in a <svelte:head> in your root layout.
	
	


</script>


<slot/>
